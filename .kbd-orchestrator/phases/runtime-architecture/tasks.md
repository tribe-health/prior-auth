<!-- generated by kbd-runtime; source revision 304 -->
# runtime-architecture tasks

## ra-01-verified-session — Return an authoritative ASO session and practice scope

- [x] 1 — 1.1 Confirm dependency completion (NONE), the assigned file ownership and applicable phase decision gates before code changes; verify canonical dependency status and retain the gate decision/evidence artifact. (COMPLETE)
- [x] 2 — 1.2 Implement verified identity resolution and the ASO membership read in one mounted session path; verify pinned Kratos native-token transport before selecting header translation. Verification: A valid browser cookie or native-token harness request selects an allowed practice must produce this observed outcome: Both yield the same sanitized scope, expiry and authorization revision without exposing credentials; fresh Postgres membership is authoritative. (COMPLETE)
- [x] 3 — 1.3 Add transaction-context setup/reset under a non-bypass DB role and a shell-neutral verified context; mirror the operation in the desktop wrapper contract. Verification: A client forges identity headers, role traits or a foreign practice must produce this observed outcome: The mounted service ignores untrusted identity hints and denies the unauthorized scope; anonymous returns 401, provider failure remains distinguishable as unavailable. (COMPLETE)
- [x] 4 — 1.4 Exercise mounted Gate/session/Postgres behavior with two synthetic identities and practices, plus unavailable Kratos and forged headers. Verification: Two identities reuse pooled database connections must produce this observed outcome: Each transaction receives only its verified context and cannot inherit the previous identity; deactivated membership is refused. (COMPLETE)
- [x] 5 — 2.1 Prove: when A valid browser cookie or native-token harness request selects an allowed practice, then Both yield the same sanitized scope, expiry and authorization revision without exposing credentials; fresh Postgres membership is authoritative. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 6 — 2.2 Prove: when A client forges identity headers, role traits or a foreign practice, then The mounted service ignores untrusted identity hints and denies the unauthorized scope; anonymous returns 401, provider failure remains distinguishable as unavailable. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 7 — 2.3 Prove: when Two identities reuse pooled database connections, then Each transaction receives only its verified context and cannot inherit the previous identity; deactivated membership is refused. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 8 — 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early. (COMPLETE)

## ra-02-durable-affirmation — Persist authenticated gate affirmation with independent controls

- [x] 1 — 1.1 Confirm dependency completion (ra-01-verified-session), the assigned file ownership and applicable phase decision gates before code changes; verify canonical dependency status and retain the gate decision/evidence artifact. (COMPLETE)
- [x] 2 — 1.2 Add an additive, checksummed server migration and least-privilege repository transaction for affirmation and its command-result record. Include the existing gate-summary read/trigger behavior and direct-column write refusal. Verification: An authorized surgeon affirms with a stable command ID must produce this observed outcome: Gate policy, AppServices capability and the database each enforce authority; one durable affirmation and audited result commit atomically. (COMPLETE)
- [x] 3 — 1.3 Remove body-selected actor authority from mounted affirmation; implement Gate/service/database refusal and the equivalent desktop wrapper contract. Verification: Administrator, agent or foreign-practice actor attempts affirmation at each enforcement layer separately must produce this observed outcome: Each layer refuses independently, even when the other two are bypassed by the test harness. (COMPLETE)
- [x] 4 — 1.4 Test fresh and upgrade installs, all three independent refusals and lost-response/payload-conflict behavior against real Postgres. Verification: The response is lost, or the same command ID carries a different payload must produce this observed outcome: A lookup/retry reconciles the committed result without a second effect; payload conflict is explicit. (COMPLETE)
- [x] 5 — 2.1 Prove: when An authorized surgeon affirms with a stable command ID, then Gate policy, AppServices capability and the database each enforce authority; one durable affirmation and audited result commit atomically. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 6 — 2.2 Prove: when Administrator, agent or foreign-practice actor attempts affirmation at each enforcement layer separately, then Each layer refuses independently, even when the other two are bypassed by the test harness. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 7 — 2.3 Prove: when The response is lost, or the same command ID carries a different payload, then A lookup/retry reconciles the committed result without a second effect; payload conflict is explicit. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 8 — 2.4 Prove: when Another authorized surgeon adds or removes a required affirmation, then The cases.gate_affirmed_at derived value updates or clears transactionally, and a caller cannot directly spoof it. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 9 — 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early. (COMPLETE)

## ra-03-clinical-command-parity — Use verified context for signing and evidence reassessment

- [x] 1 — 1.1 Confirm dependency completion (ra-02-durable-affirmation), the assigned file ownership and applicable phase decision gates before code changes; verify canonical dependency status and retain the gate decision/evidence artifact. (COMPLETE)
- [x] 2 — 1.2 Implement verified-context signing with revision/QA/source preconditions and matching Gate/service/DB refusal tests. Verification: A caller submits a forged surgeon actor or stale letter revision must produce this observed outcome: Signing is denied; request-body identity never grants authority and stale QA/signature revision cannot pass. (COMPLETE)
- [x] 3 — 1.3 Add authoritative reassessment and matching HTTP/desktop operation contracts using the existing command ledger. Verification: An authorized reassessment or signing command loses its response must produce this observed outcome: The persisted command result resolves the outcome and a repeat creates no duplicate clinical effect; signing/affirmation never enter PEM replay. (COMPLETE)
- [x] 4 — 1.4 Remove production memory authority/repositories from these mounted paths and prove command reconciliation and rollback with synthetic records. Verification: Administrator or agent directly reaches service/database signing paths must produce this observed outcome: Each independent clinical control refuses; allowed evidence changes retain met/gap/void and an audit record. (COMPLETE)
- [x] 5 — 2.1 Prove: when A caller submits a forged surgeon actor or stale letter revision, then Signing is denied; request-body identity never grants authority and stale QA/signature revision cannot pass. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 6 — 2.2 Prove: when An authorized reassessment or signing command loses its response, then The persisted command result resolves the outcome and a repeat creates no duplicate clinical effect; signing/affirmation never enter PEM replay. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 7 — 2.3 Prove: when Administrator or agent directly reaches service/database signing paths, then Each independent clinical control refuses; allowed evidence changes retain met/gap/void and an audit record. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 8 — 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early. (SKIPPED)
- [x] ra03-final-review — ra03-final-review — 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early. (COMPLETE)

## ra-04-projection-grants — Derive replica grants and FRF identity on the server

- [x] 1.1 — 1.1 Confirm dependency completion (ra-03-clinical-command-parity), the assigned file ownership and applicable phase decision gates before code changes; verify canonical dependency status and retain the gate decision/evidence artifact. (COMPLETE)
- [x] 1.2 — Record the privacy/projection contract and negative request fixtures in the mounted grant boundary; keep existing exclusions and defer criterion-label expansion. Verification: The five-table projection registry is evaluated for two practices must produce this observed outcome: Only authorized rows/columns are permitted; evidence_states explicitly uses key; remaining metadata is treated as protected unless approved otherwise. (COMPLETE)
- [x] 1.3 — Implement strict claim allowlisting, originating-session linkage, expiry and issuer/audience validation across Gate and FRF. Verification: A caller supplies table/where/columns, forged traits, headers or another service token must produce this observed outcome: None broadens the server grant; wrong issuer/audience/scope/revision is rejected. (SKIPPED)
- [x] 3 — 1.3 Implement strict claim allowlisting, originating-session linkage, expiry and issuer/audience validation across Gate and FRF. Verification: A caller supplies table/where/columns, forged traits, headers or another service token must produce this observed outcome: None broadens the server grant; wrong issuer/audience/scope/revision is rejected. (COMPLETE)
- [x] 4 — 1.4 Prove modified clients cannot broaden grant scope and required mint failure refuses access. Re-run the seven established practice-derivation cases against fresh and upgraded schema, including direct practice_id writes and parent changes. Verification: Required token minting or membership resolution fails must produce this observed outcome: Protected downstream access is denied; no permissive pass-through or invented session linkage is used. (COMPLETE)
- [x] 5 — 2.1 Prove: when The five-table projection registry is evaluated for two practices, then Only authorized rows/columns are permitted; evidence_states explicitly uses key; remaining metadata is treated as protected unless approved otherwise. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 6 — 2.2 Prove: when A caller supplies table/where/columns, forged traits, headers or another service token, then None broadens the server grant; wrong issuer/audience/scope/revision is rejected. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 2.3 — 2.3 Prove: when Required token minting or membership resolution fails, then Protected downstream access is denied; no permissive pass-through or invented session linkage is used. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 2.4 — 2.4 Prove: when A committed gate affirmation is removed in another session, then The projected cases.gate_affirmed_at clears through the authorized stream; navigation will consume this entity field, not a one-time HTTP copy. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 2.5 — 2.5 Prove: when Fresh and upgraded databases receive forged practice_id, parent-key changes and permitted parent transfers under the non-bypass role, then The existing case_evidence, evidence_citations and documents triggers overwrite forged derived practice IDs and preserve tested cascades. Cases use authorized practice ownership/RLS; evidence_states remains explicitly approved reference data without an invented practice trigger. Record the actual command, prerequisite availability and observed result. (COMPLETE)
- [x] 3.1 — 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early. (SKIPPED)
- [x] ra04-final-review — ra04-final-review — Complete RA04 applicable T0/T1, artifact-refiner and adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. (COMPLETE)

## ra-05-authorized-shape-facade — Serve Electric snapshots and continuations through the authorized facade

## ra-06-bounded-revocation — Expire and revoke active replica delivery within a measured bound

## ra-07-scoped-pem-runtime — Own hydration, status, listeners and persistence per runtime

## ra-08-committed-graph-projection — Publish committed replica batches atomically into PEM

## ra-09-pem-delivery-gate — Prove and adopt the actual PEM package artifact

## ra-10-remove-transitive-query-cache — Remove the unused bridge that brings SWR into the application

## ra-11a-sync-conformance — Prove the selected SQL materializer and commit contract

## ra-11b-worker-ownership — Own database opening, migrations and leader handover

## ra-11c-sql-materialization — Apply authorized streams and publish committed graph batches

## ra-12-public-auth-startup — Mount real public authentication and explicit startup states

## ra-13-epoch-logout-and-drafts — Fence scope changes and failed logout across tabs and reloads

## ra-14-live-evidence-timeline — Render the first authorized persisted row through graph selectors

## ra-15-attributed-annotations — Persist attributed annotations with scoped draft recovery

## ra-16-authorized-source-preview — Open cited sources through an authorized adaptive preview

## ra-17-native-session-transport — Own native credentials and verified commands in the Tauri host

## ra-18-tauri-pglite-baseline — Run the shared evidence runtime in Tauri with PGlite

## ra-19-native-sqlite-parity — Evaluate a native SQLite materializer against the PGlite baseline

## ra-20-safe-browser-updates — Coordinate compatible web code, schema and draft updates

## ra-21-safe-native-updates — Update paired native host/frontend bundles safely

## ra-22-runtime-certification — Certify the assembled runtime against the full acceptance matrix
