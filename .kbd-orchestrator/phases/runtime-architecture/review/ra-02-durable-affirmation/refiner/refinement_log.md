# RA-02 refinement log

## Iteration 1 — 2026-09-06T22:22:25Z

Specify: bounded RA-02 completion evidence, classified as `content/direct:content`. Plan: bind current sources to recorded T1 campaigns, inspect real callers and retain delivery limits. Execute: one host-boundary correction, scoped Rust verification, full schema/file/hash/checklist validation and an evidence report. Reflect: the correction routes Gate's read-only policy check through `AppServices`; the mounted server and Gate paths are real, while browser and desktop consumers remain test-only. Persist: manifest, constraints, report, checklist, state and actual validator output.

Convergence requires deterministic validation to return exit 0 and the independent adversarial review to run afterward. No broad phase or release tier, deployment, UI mount, Tauri registration or publication belongs to this iteration.

## Iteration 2 — 2026-09-06T22:49:56Z

Specify: resolve the complete-packet judge's one critical and three warning findings. Plan: preserve principal provenance, make the hook signature type-safe, reuse Gate's hardened client, and bind both restricted database URLs to one parsed endpoint. Execute: implemented each correction and added focused controls. Reflect: the critical agent-principal path now refuses before membership even when the injected membership accepts; all four findings have direct source and test evidence. Persist: refreshed source hashes, report, checklist, decisions, state and validation output.

Convergence: terminate after the deterministic refiner and a new fresh-context adversarial review both pass. The incomplete first packet and the complete pre-fix finding set remain review evidence rather than being silently discarded.

## Iteration 3 — 2026-09-06T23:08:30Z

Specify: resolve the post-fix judge's command-order critical and stale-correlation warning. Plan: resolve an existing identity/practice-scoped command before target-case authority in both independent execution layers, clear the hook's correlation only on definitive refusal, and prove missing and foreign changed-case conflicts against real PostgreSQL. Execute: updated `AppServices`, the SQL function, transaction controls, the hook and its tests; reran fresh, upgrade and mounted T1 campaigns. Reflect: conflict semantics no longer vary with a substituted case's visibility, while receipts remain opaque across identity and practice boundaries. Persist: refreshed design text, current hashes, report, checklist, state and validator output.

Convergence requires this third deterministic validation and one new isolated adversarial review to return without a critical finding. The previous finding receipts remain immutable review history.
