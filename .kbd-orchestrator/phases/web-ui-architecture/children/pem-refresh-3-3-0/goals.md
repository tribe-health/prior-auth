# Goals — web-ui-architecture› pem-refresh-3-3-0

- G1 · React binding gets its framework-consistent name. Publish @prometheus-ags/entity-graph-react as the real React binding (source stays in packages/entity-graph-react). Republish @prometheus-ags/prometheus-entity-management as a thin alias that re-exports all three entrypoints — '.', './devtools', './devtools/auto' — then npm-deprecate it with a pointer. Nothing downstream breaks. Update scripts/public-packages.mjs, release/v3-release-contract.json artifacts + versionPolicy.npm.packages, and .changeset/config.json fixed[].
- G2 · 3.3.0 lockstep across the fixed group. All 12 existing npm packages plus the new entity-graph-react move 3.2.0 to 3.3.0 together, via changesets. entity_graph_flutter moves 3.1.0 to 3.3.0 to end the JS/Dart version skew.
- G3 · react-table v8 to v9 migration, feature-by-feature. 15 call sites: packages/entity-graph-react/src/ui/entity-table.tsx and columns.tsx (including the declare-module ColumnMeta augmentation), plus 10 files across examples/nextjs-app and examples/vite-app. useReactTable becomes useTable; getCoreRowModel is automatic; getSortedRowModel becomes createSortedRowModel in an explicit tableFeatures object. Do NOT use stockFeatures — TanStack documents it as a migration shortcut, not a production end state.
- G4 · PGlite to a caret range on latest. @electric-sql/pglite moves from the pinned 0.5.4 devDependency to ^0.5.8 in entity-graph-core, and pglite-persistence.integration.test.ts must pass against it. This is the adapter prior-auth depends on for its ElectricSQL sync path.
- G5 · Flutter to latest, on hooks_riverpod. Swap flutter_riverpod for hooks_riverpod 3.4.3, add flutter_hooks 0.21.3+1, and lift riverpod_annotation to 4.0.7, riverpod_generator to 4.0.9, build_runner to 2.16.1, riverpod_lint to 3.1.9. These match the prior-auth mobile app's pins exactly. The current pubspec comments justify the older pins against Flutter 3.44.8 — re-verify that constraint before overriding it and record what was actually observed.
- G6 · Dart hook helpers mirroring the React surface. Add use-entity, use-entity-list and use-entity-query to entity_graph_flutter so the Dart API is shaped like the React binding's. hooks_riverpod re-exports flutter_riverpod, so existing ConsumerWidget consumers keep working.
- G7 · Full dependency audit. Verified stale as of 2026-09-04: react-table 8 to 9 (G3) and pglite 0.5.4 to 0.5.8 (G4). Caret ranges already absorb react-virtual, immer, lucide-react, zustand, tailwind-merge. Optional peers @ag-ui/core, @tauri-apps/plugin-sql and loro-crdt use open >= ranges — confirm the floors are still honest rather than raising them reflexively.
- G8 · Release gates must pass before publish. pnpm run validate:release-contract (baseline was 0 errors, 16 artifacts, 12 npm packages), verify:package-contracts, verify:no-workspace-leak, verify:binding-singletons, verify:skills, and release:check. The alias package must satisfy PACKAGE_ENTRYPOINT_CONTRACT in scripts/public-packages.mjs.
