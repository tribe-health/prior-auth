# Goals

- G1 · App shell and navigation: implement the prototype's 10-step case pipeline and role-gated global nav as a React 19 + react-router 7 shell, with layout, theme wiring from the generated tokens, error and loading boundaries, and the gated-step model (surgeon gate blocks steps 07-10). Source: docs/design/prototype/assets/shell.js.
- G2 · Base component library under web/src/shared/ui: promote the 61 shadcn primitives in web/src/components/ui into an intentional, token-driven library. Every file kebab-case. Evidence-state treatments render met/gap/void with a text label — colour is reinforcement, never the signal. Ember #DF7C35 never on body-size text; use #A85417.
- G3 · Entity graph and store architecture: bind @prometheus/entity-graph-core + entity-graph-react as the normalized entity layer, with Zustand holding transient interaction state ONLY. Lists hold ordered identifiers; every view re-joins the canonical record. No query cache — audit.sh check 2 stays green.
- G4 · Local-first PGlite plan: PGlite in the browser as the local store, replicated Postgres to PGlite via prometheus-entity-sync (pes-server, PSyncV1 over WebSocket + MessagePack) using entity-graph-sync. Privacy class per table; local data structurally refused at the sync boundary, never filtered.
- G5 · Reference vertical slice: build evidence-timeline end to end — api, hooks, model, components — as the pattern every remaining feature copies. It is the only screen exercising all three evidence states. All other routes stay placeholders.
- G6 · docker-compose.yaml: flint-forge with its postgres18 image, flint-gate, flint-realtime-fabric, prometheus-entity-sync, and prometheus-entity-management. Built from /Users/gqadonis/Projects/prometheus/. PHI stays inside the practice boundary.
- G7 · Architecture decisions recorded: ADRs for the component model, navigation and gating strategy, entity-graph binding, and local-first sync topology. No implementation beyond the shell, the library, and the single slice.
