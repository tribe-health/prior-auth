## Why

The runtime assessment identifies a missing integration or invariant in this area. Run the shared evidence runtime in Tauri with PGlite is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Establish the PGlite baseline in the actual Tauri application before selecting another engine. Coordinate one database/sync owner across windows using an explicit host-controlled ownership service and validate supported persistence rather than assuming browser behavior transfers to every webview.

## Capabilities

### New Capabilities

- `ra-18-tauri-pglite-baseline`: Run the shared evidence runtime in Tauri with PGlite.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

ASO: desktop/webview composition, native runtime ownership and web storage adapter; PEM consumed runtime contracts.

Dependencies: ra-17-native-session-transport. Runtime order: 5. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
