## Why

The runtime assessment identifies a missing integration or invariant in this area. Coordinate compatible web code, schema and draft updates is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Version API, shape, logical schema, engine format, graph snapshot and app contracts independently. Stage immutable assets and activate only at a safe user-work boundary; coordinate old tabs/schema leases and use expand/migrate/contract on the server.

## Capabilities

### New Capabilities

- `ra-20-safe-browser-updates`: Coordinate compatible web code, schema and draft updates.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

ASO: runtime update coordinator, compatibility service/manifest and browser deployment assets; server migration deployment job; scoped draft and replica services.

Dependencies: web-case-to-letter/web-17-browser-scenario-certification. Runtime order: 6. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
