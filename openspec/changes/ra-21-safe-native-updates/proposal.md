## Why

The runtime assessment identifies a missing integration or invariant in this area. Update paired native host/frontend bundles safely is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Stage matching signed native host/frontend versions, reconcile work, quiesce sync and relaunch into validated migrations. Never hot-replace privileged renderer JavaScript from the website; rollback requires compatibility with current data or an approved rebuild.

## Capabilities

### New Capabilities

- `ra-21-safe-native-updates`: Update paired native host/frontend bundles safely.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

ASO: desktop updater integration, host migration/compatibility coordinator and deployment bundle contract.

Dependencies: ra-19-native-sqlite-parity, ra-20-safe-browser-updates. Runtime order: 6. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
