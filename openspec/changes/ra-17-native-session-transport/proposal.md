## Why

The runtime assessment identifies a missing integration or invariant in this area. Own native credentials and verified commands in the Tauri host is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Implement one native session owner and protected credential facility, sanitized renderer projection and per-invocation session-generation/scope checks. Bind all existing HTTP operations to matching host commands; keep the same React feature hooks and model.

## Capabilities

### New Capabilities

- `ra-17-native-session-transport`: Own native credentials and verified commands in the Tauri host.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

ASO: desktop Tauri host/session/IPC and web composition-root transport adapter; shared AppServices contracts; Gate native credential path.

Dependencies: ra-16-authorized-source-preview. Runtime order: 5. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
