## 1. Eligibility and implementation

- [x] 1.1 Confirm dependency completion, assigned file ownership, frozen web-00 contracts, and applicable decision gates before edits; verify canonical KBD/OpenSpec state and retain the result.
- [x] 1.2 Create synthetic request, corrected-resubmission, and clinical-appeal inputs plus immutable IDs/hashes/expected outputs; verify no real PHI.
- [x] 1.3 Build local stack setup and actual-browser runner against current source; verify it detects missing services and skipped prerequisites.
- [x] 1.4 Encode expected route, state, prose, citation, error, responsive, restart, and session outcomes without running the broad campaign.

## 2. Focused verification

- [x] 2.1 Run focused runner/manifest T1, artifact-refiner, and adversarial review; do not claim Tier 2 passed.

## 3. Completion evidence

- [x] 3.1 Record actual commands and observed outputs, confirm real mounted callers at this change boundary, update documentation/spec deltas, and mark only satisfied work complete.

## 12. Approved agent integration additions — pending registration

These additions follow [revision 12](../../../docs/handoff/web-case-to-letter-revision-12-agent-integration.md) after web UI acceptance.
They supersede incompatible older composer/inference/channel scope; checked
historical tasks do not establish their completion. Register through KBD rather
than editing generated counters.

- [x] 12.1 Add internal assembly and authenticated Liter-LLM services, readiness checks and server-side secret injection; freeze template package digest in the fixture manifest.
- [x] 12.2 Provide isolated initial/correction/appeal synthetic cases, deterministic recorded model responses and a separate live Qwen streaming/tool smoke fixture.
- [x] 12.3 Prove repeat initialization preserves existing identity, credentials, signatures, clinical records and revisions; never silently update expected package digests or reset cases.
