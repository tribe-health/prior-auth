## Why

The runtime assessment identifies a missing integration or invariant in this area. Serve Electric snapshots and continuations through the authorized facade is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Implement the approved shape route through Gate→FRF→private Electric, including initial snapshot, handles, offsets, control messages, errors and refetch. Authorization applies to every request and handle binding. The certified composition has no client-reachable upstream bypass.

## Capabilities

### New Capabilities

- `ra-05-authorized-shape-facade`: Serve Electric snapshots and continuations through the authorized facade.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

FRF: gateway/application/port modules for a proposed Electric facade and outbound adapter; ASO: compose and Gate route configuration.

Dependencies: ra-04-projection-grants. Runtime order: 2. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
