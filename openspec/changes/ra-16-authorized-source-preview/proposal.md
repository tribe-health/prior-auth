## Why

The runtime assessment identifies a missing integration or invariant in this area. Open cited sources through an authorized adaptive preview is required to achieve the phase's authorized, scoped runtime outcome.

## What Changes

Fetch bounded document bytes through an audited subject/practice/document access service; never expose storage_uri as ambient authority. Integrate an accessible adaptive preview and source-location actions into the timeline, with epoch cancellation and object-URL cleanup.

## Capabilities

### New Capabilities

- `ra-16-authorized-source-preview`: Open cited sources through an authorized adaptive preview.

### Modified Capabilities

None. The root OpenSpec spec store has no existing capability to modify.

## Impact

ASO: attachment authorization/byte service and desktop wrapper, evidence source-preview hooks/parts, shared adaptive dialog/sheet and token source if needed.

Dependencies: ra-15-attributed-annotations. Runtime order: UI 3. Recommended agent: Codex; complexity L, High; model class frontier.

See the reviewed [phase plan](../../../.kbd-orchestrator/phases/runtime-architecture/plan.md) and its decision gates. This proposal authorizes no implementation during planning.
