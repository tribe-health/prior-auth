## 1. Eligibility and bounded implementation

- [x] 1.1 Confirm dependency completion (ra-13-epoch-logout-and-drafts, ra-10-remove-transitive-query-cache), the assigned file ownership and applicable phase decision gates before code changes; verify canonical dependency status and retain the gate decision/evidence artifact.
- [x] 1.2 Wire scoped graph selectors and intent commands into the existing timeline; replace citation-absence-as-void and the false gate placeholder with explicit semantic/committed states. Verification: A synthetic Postgres row is inserted then updated/deleted through the authorized source must produce this observed outcome: The existing timeline and a second subscriber update from the same graph, with no reload or copied clinical React state; SQL/graph/network/rendered values agree.
- [x] 1.3 Recheck every newly exported runtime adapter's caller and use the actual installed package entrypoint. Verification: Wrong-practice or broadened-column requests are attempted, including continuation must produce this observed outcome: They are denied and excluded fields never arrive; all three reference state identities remain distinct.
- [x] 1.4 Capture the real synthetic Postgres→Gate→FRF→Electric→SQL→graph→browser path, denied payloads, update/delete and logout evidence; keep screenshots/logs synthetic. Verification: The browser exercises logged-out start, account change, reduced motion and resize must produce this observed outcome: Public routes remain usable, old protected state disappears immediately, per-view state survives ordinary resize, and no protected document-transition snapshot remains.

## 2. Behavioral acceptance

- [x] 2.1 Prove: when A synthetic Postgres row is inserted then updated/deleted through the authorized source, then The existing timeline and a second subscriber update from the same graph, with no reload or copied clinical React state; SQL/graph/network/rendered values agree. Record the actual command, prerequisite availability and observed result.
- [x] 2.2 Prove: when Wrong-practice or broadened-column requests are attempted, including continuation, then They are denied and excluded fields never arrive; all three reference state identities remain distinct. Record the actual command, prerequisite availability and observed result.
- [x] 2.3 Prove: when The browser exercises logged-out start, account change, reduced motion and resize, then Public routes remain usable, old protected state disappears immediately, per-view state survives ordinary resize, and no protected document-transition snapshot remains. Record the actual command, prerequisite availability and observed result.

## 3. Completion evidence

- [x] 3.1 Complete applicable T0/T1 and the phase-prescribed artifact-refiner then adversarial review; preserve synthetic evidence, confirm real callers and mark only actually satisfied work complete. Do not run broad phase/release tiers early.
