# Web-03 administering-entity resolution result

Result: **Passed** for the focused change boundary.

The mounted browser case-detail view now resolves and displays the controlling
administering entity and versioned coverage path through the typed HTTP,
`AppServices`, and PostgreSQL command chain. Parked outcomes remain explicit
and block downstream work. Authoritative input changes invalidate the current
resolution, and uncertain browser commands reconcile by exact command ID and
expected case-input revision before current state is accepted.

Resolution and authoritative-input mutations now acquire table and case locks
in one order, including the first resolution when no current row exists.
Future-effective plans, enrollments, and rules remain `missing` for the case
service date; disjoint historical windows remain `missing`; `expired` applies
only after a non-empty intersected path has ended.
The panel labels the returned `validTo` date as exclusive, and an ended plan,
enrollment, or rule is described neutrally as an expired coverage path.

Focused evidence passed 52 browser tests across thirteen files, TypeScript typecheck, lint, five
mounted Axum tests, Rust checks and formatting, strict OpenSpec validation,
fresh and populated-upgrade PostgreSQL/AppServices lifecycle probes, and the
artifact-refiner checks.

The PostgreSQL race control failed with a deadlock and an uninvalidated row
when the input-table lock was deliberately removed. Restoring the lock passed
resolution-first and mutation-first interleavings. Both final lifecycle probes
also passed future plan, enrollment, and rule classification.

The final critic repairs return incomplete inputs as HTTP 422 with the frozen
guidance, keep that guidance visible after routing the user to intake, and focus the first missing resolution
field only after intake loads the authorized full case record. This preserves
the case-summary publication boundary. They also persist and display the entity, plan, enrollment,
rule, and source-document versions on current-resolution reads. A committed
receipt followed by authoritative invalidation now clears the saved command
and returns the view to unresolved. The final browser suite passes 52 tests
across thirteen files. It includes production route composition, the typed
full-record API/hook path, the narrow PEM selector, and over-wide-row refusal
at both transport and PGlite materialization boundaries. A mounted Axum test
proves protected case-detail inputs require `case:read`, verified practice
scope, and an authorized target. Post-freeze independent artifact and distinct-model review outcomes
are recorded beside the exact frozen manifest before KBD completion. Earlier
79-snapshot reviews remain historical task-5 evidence and do not qualify the
final source set.

This result does not certify the complete browser workflow. Document upload,
criteria selection, evidence assembly, both letter paths, the assembled
fixture, and actual-browser certification remain Web-04 through Web-17.
