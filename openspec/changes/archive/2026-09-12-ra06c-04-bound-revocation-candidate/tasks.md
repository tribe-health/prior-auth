## 1. Candidate identity

- [x] 1.1 Implement and validate the canonical source/runtime manifest with explicit exclusions and secret-safe configuration hashing.
- [x] 1.2 Build immutable runtime artifacts, freeze source/artifacts/config, and require every command receipt to name the manifest digest.

## 2. Local assembled campaign

- [x] 2.1 Run application logout, direct Kratos admin revocation, membership change and expiry against a nonempty active FRF body and two Gate replicas.
- [x] 2.2 Run stale refill, stalled consumer, lost/reordered notification, Redis partition/restart/restore and foreign-tenant/deployment scenarios.
- [x] 2.3 Verify synchronous Zustand fencing and stable command ownership in wide and compact shared React layouts; keep the missing materializer caller blocking in amended RA11c.

## 3. Completion and parent return

- [x] 3.1 Close logs, write a separate evidence index, run applicable current T0/T1, strict OpenSpec validation, artifact-refiner and isolated adversarial review.
- [x] 3.2 Write the child handoff mapping parent and assessment findings to callers and receipts; require parent T2 and renewed RA06 review against the same digest.
