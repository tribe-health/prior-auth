{#- Draft banner. Present on every unsigned render; render_signed() never
    strips it — a signed export is produced by the host from the signed body
    with the banner removed by the export step, never by a template. -#}
> **DRAFT — NOT SIGNED.** Review the source citations before approval.
